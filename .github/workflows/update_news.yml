name: Auto Japan News

on:
  schedule:
    - cron: '0 * * * *' # 每小时运行一次
  workflow_dispatch: # 允许手动触发
  push:
    branches:
      - main

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        # 使用 token 确保有权限拉取和推送
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run scraper
        run: |
          python main.py

      - name: Commit and push changes
        run: |
          # 1. 配置 Git 用户信息
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # 2. 确保添加了需要提交的文件
          git add data.json archive/ 
          
          # 3. 检查是否有文件更改 (如果文件有变动)
          if git diff --staged --quiet; then
            echo "No changes in data to commit. Skipping push."
          else
            # 4. 提交更改，移除 [skip ci] 标签，确保 Cloudflare 触发部署
            # 我们将标签改为 [data update]，Pages 不会跳过这个标签
            git commit -m "Update news data [data update]" 
            
            # 5. 使用 git push --force 强制覆盖远程仓库
            echo "Attempting to force push to main branch..."
            git push origin main --force
          fi